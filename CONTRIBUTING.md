# Contributing

PRs welcome. Please:

1. Open an issue for non-trivial changes first.
2. Run `pnpm test` and `pnpm build` before submitting.
3. Keep changes focused. No drive-by formatting.
4. Follow existing code style (no emojis in code, no obvious-code comments).
